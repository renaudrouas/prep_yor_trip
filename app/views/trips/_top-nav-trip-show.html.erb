<nav>
  <ul class="nav nav-tabs">
    <li role="presentation">
      <% if @travels.empty? %>
        <%= link_to new_trip_travel_path(@trip) do %>
          <i class="fa"><%= image_tag "travel.png", class: "icon-top-nav" %></i> Transports
        <% end %>
      <% else %>
        <%= link_to trip_travels_path(@trip) do %>
          <i class="fa"><%= image_tag "travel.png", class: "icon-top-nav" %></i> Transports
        <% end %>
      <% end %>
    </li>
    <li role="presentation">
      <% if @accomodations.empty? %>
      <%= link_to new_trip_accomodation_path(@trip) do %>
      <i class="fa"><%= image_tag "maison.png", class: "icon-top-nav" %></i> Hébergements
      <% end %>
      <% else %>
      <%= link_to trip_accomodations_path(@trip) do %>
      <i class="fa"><%= image_tag "maison.png", class: "icon-top-nav" %></i> Hébergements
      <% end %>
      <% end %>
    </li>
    <li role="presentation">
      <% if @diaries.empty? %>
      <%= link_to new_trip_diary_path(@trip) do %>
      <i class="fa"><%= image_tag "note-2.png", class: "icon-top-nav" %></i> Note
      <% end %>
      <% else %>
      <%= link_to trip_diaries_path(@trip) do %>
      <i class="fa"><%= image_tag "note-2.png", class: "icon-top-nav" %></i> Note
      <% end %>
      <% end %>
    </li>
    <!--link scroll down  -->
    <li role="presentation" >
      <% if @tasks.empty? %>
      <%= link_to new_trip_task_path(@trip) do %>
      <i class="far"><%= image_tag "checklist.png", class: "icon-top-nav" %></i> Checklist
      <% end %>
      <% else %>
      <%= link_to trip_path(@trip) do %>
      <i class="far"><%= image_tag "checklist.png", class: "icon-top-nav" %></i> Checklist
      <% end %>
      <% end %>
    </li>
  </ul>
</nav>
