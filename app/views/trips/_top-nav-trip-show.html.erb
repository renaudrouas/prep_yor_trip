<!--   <div class="wrapper">
</div> -->
<nav>
  <ul class="nav nav-tabs">
    <li role="presentation">
      <% if @travels.empty? %>
        <%= link_to new_trip_travel_path(@trip) do %>
          <i class="fa fa-plane"></i> Transports
        <% end %>
      <% else %>
        <%= link_to trip_travels_path(@trip) do %>
          <i class="fa fa-plane"></i> Transports
        <% end %>
      <% end %>
    </li>
    <li role="presentation">
      <% if @accomodations.empty? %>
      <%= link_to new_trip_accomodation_path(@trip) do %>
      <i class="fa fa-home"></i> Hébergements
      <% end %>
      <% else %>
      <%= link_to trip_accomodations_path(@trip) do %>
      <i class="fa fa-home"></i> Hébergements
      <% end %>
      <% end %>
    </li>
    <li role="presentation">
      <% if @diaries.empty? %>
      <%= link_to new_trip_diary_path(@trip) do %>
      <i class="fa fa-book"></i> Note
      <% end %>
      <% else %>
      <%= link_to trip_diaries_path(@trip) do %>
      <i class="fa fa-book"></i> Note
      <% end %>
      <% end %>
    </li>
    <li role="presentation" >
      <% if @diaries.empty? %>
      <%= link_to new_trip_task_path(@trip) do %>
      <i class="far fa-check-square"></i> Checklist
      <% end %>
      <% else %>
      <%= link_to trip_tasks_path(@trip) do %>
      <i class="far fa-check-square"></i> Checklist
      <% end %>
      <% end %>
    </li>
  </ul>
<nav>
